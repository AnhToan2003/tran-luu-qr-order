# ===== Tran Luu QR Order System - Environment Variables =====
# Copy file này thành .env và điền giá trị thực

# MongoDB Connection URI (Railway MongoDB hoặc MongoDB Atlas)
MONGO_URI=mongodb://localhost:27017

# Server Port (Railway tự set PORT tự động)
PORT=3001

# Node Environment
NODE_ENV=production
